# TD_Game_Project

a little tower defence game project in C# from scratch for windows and linux

build for windows : dotnet publish -r win-x64 --self-contained true -c Release

build for linux : dotnet publish -r linux-x64 --self-contained true -c Release